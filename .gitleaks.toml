[extend]
useDefault = true

[[allowlists]]
description = "Allow idempotency_key fixtures in backend MCP tests only"
paths = ['''backend/tests/test_mcp\.py''']
regexTarget = "line"
regexes = ['''"idempotency_key"\s*:\s*"[A-Za-z0-9_-]{8,}"''']
