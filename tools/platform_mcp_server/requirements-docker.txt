# Slim set for Docker (python:3.11-slim). Avoids packages that often fail to build
# or install on minimal images (snowflake, chromadb, databricks-sql-connector, pymssql, weaviate).
# Those tools still work if you pip install -r requirements.txt on a full dev image.
#
# Full local / custom image: pip install -r requirements.txt

fastapi==0.115.0
uvicorn[standard]==0.32.0
httpx==0.27.2
psycopg2-binary==2.9.9
pymysql==1.1.2
boto3>=1.28.0,<2
openai>=1.0.0,<2
pinecone>=5.0.0,<6
slack-sdk>=3.27.0
PyGithub>=2.2.0
notion-client>=2.2.0
azure-storage-blob>=12.19.0,<13
google-cloud-bigquery>=3.14.0,<4
google-cloud-storage>=2.14.0,<3
qdrant-client>=1.7.0,<2
pandas>=2.0.0,<3
